apiVersion:  v1
kind:  Service
metadata:
  name:  postgres-service
  labels:
    app: postgres-service
  namespace: k8s-demo
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432
