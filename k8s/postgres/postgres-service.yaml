apiVersion:  v1
kind:  Service
metadata:
  name:  postgres
  labels:
    app: postgres_service
  namespace: k8s-demo
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432
